spring.datasource.url= jdbc:postgresql://localhost:5432/backendINKOM
spring.datasource.username= postgres
spring.datasource.password=wachtwoordJelmer!

#comment out after first creation of database
# spring.sql.init.mode=always
# spring.datasource.initialization-mode=always

# comment in after first creation of datebase
spring.datasource.initialize=false

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

# App Properties
#novi.sec.jwtSecret= noviHogeschoolSecretKey
INKOM.sec.jwtSecret= databaseJelmerINKOM
INKOM.sec.jwtExpirationMs= 86400000
